/**
 * Enum for available app environments.
 *
 * @enum {string}
 */
export const enum Environments {
  DEVELOPMENT = 'development',
  PRODUCTION = 'production',
  STAGING = 'staging',
  TEST = 'test',
}
